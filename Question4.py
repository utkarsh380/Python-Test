create table User(
   user_id INT NOT NULL AUTO_INCREMENT,
   username VARCHAR(100) NOT NULL,
   password VARCHAR(40) NOT NULL,
   PRIMARY KEY ( user_id )
   FOREIGN KEY (address_id) REFERENCES Address(address_id)
);

create table Address(
   address_id INT NOT NULL AUTO_INCREMENT,
   street VARCHAR(100) NOT NULL,
   pincode VARCHAR(40) NOT NULL,
   country VARCHAR(100) NOT NULL,
   state VARCHAR(40) NOT NULL,
   phone_no VARCHAR(40) NOT NULL,
   PRIMARY KEY ( address_id )
);

SELECT User.user_id, User.username, Address.street, Address.pincode, Address.country, Address.state, Address.phone_no,
FROM User
INNER JOIN Address ON User.user_id=Address.address_id;
































































































































































































